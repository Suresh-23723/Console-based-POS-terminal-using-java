package Tax_Calculation;

public class TaxCalculator {
    public double calculateTax(double amount) {
        // logic to calculate tax based on the amount
        //tax percent is 12
        double tax = amount*0.12;
        return tax;
    }
}
